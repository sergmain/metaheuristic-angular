issue-1209
переработать страницу создания экспериментов в соответствии со стандартным интерфейсом,
 урл - http://localhost:8080/dispatcher/ai/experiment/experiments

результаты экспериментов получать по урл /rest/v1/dispatcher/experiment-result
страница "Результаты экспериментов" не должна содержать слово Atlas

везде в коде вместо ai.Atlas должно быть ai.ExperimentResult или ai.Experiment.Result или как-то по другому но без Atlas


issue-1216
http://localhost:8080/dispatcher/company/batch/company-batches/2
при использовании пользователя с ролью MASTER_SUPPORT или MASTER_OPERATOR реализовать возможность удаления пакетов группами. 
Т.е. сценарий такой - юзер отмечает галками пакеты которые он хочет удалить.
Затем нажимает кнопку "Удалить отмеченное" (сейчас этой кнопки нет) и после подтверждения удаления выбранных пакетов,
выполняется запрос к серверу на удалние списка пакетов.



issue-1217
http://localhost:8080/dispatcher/company/companies
реализовать управление компаниями
для того, чтобы посмотреть как это работает надо использовать юзера master-admin
который задается в application.properties

rest url - /rest/v1/dispatcher/company



issue-1238
ангулар
http://localhost:4200/#/batch
сделать кнопку 'Task states' точно такую же как в списке ExecContext
урл тот же самый но код страницы скопировать или сделать так чтобы он открывался по другому урл.
т.е. идея в том что урл должен относиться к Пакетам, а не к Контрольной панели


issue-1239
ангулар
http://localhost:4200/#/experiments
сделать кнопку 'Task states' точно такую же как в списке ExecContext
урл тот же самый но код страницы скопировать или сделать так чтобы он открывался по другому урл.
т.е. идея в том что урл толжет относиться к Пакетам, а не к Контрольной панели


issue-1240
angular
http://localhost:4200/#/batch
в столбце "Исходный код Имя файла" должно выводиться 2 значения:
- uid SourceCode
- имя файла

сейчас выводится только имя файла


issue-1241
откатить назад issue-1229:
        angular
        http://localhost:4200/#/dispatcher/source-codes/1353/exec-contexts
        убрать кнопку создания контекста Add new context

т.е. вернуть кнопку


issue-1242
angular
http://localhost:4200/#/dispatcher/accounts/access/21
изменение ролей не работает. после нажатия Save роли не сохраняются

    /**
     *
     * @param accountId Account.id
     * @param role name of role to set or remove
     * @param checkbox flag to set a role or to remove it
     * @param companyId Account.companyId
     * @return see @ai.metaheuristic.api.data.OperationStatusRest
     */
    @PostMapping("/company-account-edit-roles-commit/{companyId}")
    @PreAuthorize("hasAnyRole('MASTER_ADMIN')")
    public OperationStatusRest rolesEditFormCommit(Long accountId, String role, @RequestParam(required = false, defaultValue = "false") boolean checkbox,
                                      @PathVariable Long companyId) 

в отличие от стандартного интерфейса надо передавать название роли, а не ее индекс
