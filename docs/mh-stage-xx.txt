X. Billing ===============
X.X - реализовать новый модуль Billing.
  TODO - предоставить rest урл для Billing

issue-xxxx
TODO разобраться, каккие роли должны иметь доступ к чему
проверка изменения статусов пакетов должна быть реализована только для ролей operator, manager, admin
т.е. для тех у кого есть права на доступ к интерфейсу пакетов

для всех других ролей статус не заправшивать

как следствие, если сессия устарела и проихошло отключение, то и проверки статусов пакетов должны прекращаться,
даже если отключенный польховаетель был из списка разрешенных ролей

сейчас
если зайти под учеткой master_operator по выдается ошибка 403 в Доступе отказано

issue-xxxx
для авторизованного юзера сохранять в локальном хранилище выбранные значения локали (языка) и темы
выбранные значения должны восстанавливаться после того, как юзер снова залогируется

при реализации надо учитывать факт, что на одном компе могут быть залогинены разные пользователи и, соответственно,
надо сохранять настройки для каждого юзера персонально
