---- bug-801
url - http://localhost:4200/launchpad/experiments/651/edit
при удалении снипета вызывать урл
/rest/v1/launchpad/experiment/experiment-snippet-delete-by-type-commit/{experimentId}/{snippetType}

и передавать тип снипета, а не его код

сейчас для удаления вызывается /rest/v1/launchpad/experiment/experiment-snippet-delete-commit/{experimentId}/{snippet}

---- bug-802
определять по какому соединению идет обращени - по http Или https
если соединение устанавливается по http, то делать принудительный редирект на https
для контроля этого поведения надо сделать глобальную переменную isSslRequired,
которая будет определять нужно ли делать редирект или нет.
данную переменную поместить
в environment.ts со значением false
в environment.production.ts со значением true

---- bug-803
url - /launchpad/batch
сейчас отсутствуют средства для перезапроса данных с сервера. чтобы получить актуальное состояние
надо покинуть страницу со списком пакетов и вернуться назад

что нужно
необходимо добавить перезапрос данных при нажатии пункта меню Пакеты, которое находится слева


---- bug 804
Добавить обработку нажания кнопки надад и вперед
для примера:
https://www.bennadel.com/blog/3533-using-router-events-to-detect-back-and-forward-browser-navigation-in-angular-7-0-4.htm
https://angular.io/guide/router

сейчас:
 пр нажатии кнопки назад выдается сообщение 404 т.к страница на самом деле не существует

надо:
 при нажатии кнопки назад корректно переходим на предыдущий урл

доп инфа:
оно всегда работало (и работает) когда сервером является nodejs, который обсепечивает перезапись урла
у нас сейчас все лежит на гитхабе где нет никакой перезаписи урлов поэтому 404 и выдется
надо реализовать сохранение истории урлов в браузере

как воспроизвести:
- поставить любой сервер, не nodejs
- запустить ng build --aot --prod=true
- результат из дира distr поместить на этот сервер
- перейти по любому урл, который не индекс и нажать рефрешь



---- bug 805
урл - http://localhost:4200/launchpad/plans/1329/workbooks
не удаляются Workbook из планов
сейчас:
если в списку планов перейти на страницу workbook и нажать кнопку Удалить, то кнопка становися серой,
 но ничего не удаляется

надо:
при нажатии кнопки удалить Workbook должно происходить:
 - открывается форма подтвержения удаления
 - если удаление подтерждено, то удаляется
 - иначе форма подтерждения закрывается

